#include "header.h"
#include "head_backup.h"

int match_config(uint8_t buf_i, void *buf_arr[], Bool backup_data_arr[NUM_DATA_BACKUP], struct filename_type *filename_data)
{
	Bool data_backed_up = False;

	if (backup_data_arr[gitconfig_data])
	{
	}

	return 0;
}
